[package]
name = "atlas"
description = "Art conformance gate: bakes the palette, validates sprite sets, generates placeholders, quantises renders."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[[bin]]
name = "atlas"
path = "src/main.rs"

[dependencies]
png = "0.17"
ron = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
